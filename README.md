# change-session

`cs path/to/my/project` opens a new tmux session in that folder, or opens an existing one if the session existed already.

If you are already within a tmux session, the command will switch to the session.

## Installation

`go install EskelinenAntti/go-to/cmd/cs@latest`
